# Agent-specific user-owned policy additions for layered agentbox sandboxes.
#
# This file is merged after the shared policy for the matching active agent.
# Apply edits without restarting: agentbox proxy reload (from the host).
#
# See docs/policy/schema.md for the full format reference.
# Use `merge_mode: replace` on a domain entry here to replace the accumulated
# record for the same host from earlier layers.

services: []
domains: []
