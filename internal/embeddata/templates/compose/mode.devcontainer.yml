# Managed by agentbox.
# Re-run `agentbox init --mode devcontainer` or `agentbox switch --agent <name>` to regenerate this file.
# Do not edit this file directly. Use .agent-sandbox/compose/user.override.yml or user.agent.<agent>.override.yml instead.
services:
  proxy:
    volumes:
      - type: bind
        source: ../policy/policy.devcontainer.yaml
        target: /etc/agent-sandbox/policy/devcontainer.policy.yaml
        read_only: true
        bind:
          create_host_path: false
  agent:
    volumes:
      - type: bind
        source: ../../.devcontainer
        target: /workspace/.devcontainer
        read_only: true
        bind:
          create_host_path: false
