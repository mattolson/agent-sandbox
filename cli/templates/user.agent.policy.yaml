# Agent-specific user-owned policy additions for layered CLI sandboxes.
#
# This file is merged after the shared policy for the matching active agent.
# Changes take effect after switching to this agent and restarting the proxy.
#
# See docs/policy/schema.md for the full format reference.

services: []
domains: []
