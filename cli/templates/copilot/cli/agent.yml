# Managed by agentbox.
# Re-run `agentbox init` or `agentbox switch --agent <name>` to regenerate this file.
# Do not edit this file directly. Use .agent-sandbox/compose/user.override.yml or user.agent.copilot.override.yml instead.
services:
  proxy:
    volumes: []
  agent:
    image: ghcr.io/mattolson/agent-sandbox-copilot:latest
    volumes:
      # Persist Copilot credentials
      - copilot-state:/home/dev/.copilot
      # Persist shell history
      - copilot-history:/commandhistory
volumes:
  copilot-state:
  copilot-history:
