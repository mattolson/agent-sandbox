# Managed by agentbox.
# Re-run `agentbox init --mode devcontainer` or `agentbox switch --agent <name>` to regenerate this file.
# Do not edit this file directly. Use .agent-sandbox/compose/user.override.yml or user.agent.<agent>.override.yml instead.
services:
  proxy:
    volumes:
      - ../policy/policy.devcontainer.yaml:/etc/agent-sandbox/policy/devcontainer.policy.yaml:ro
  agent:
    volumes:
      - ../../.devcontainer:/workspace/.devcontainer:ro
