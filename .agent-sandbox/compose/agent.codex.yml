# Managed by agentbox.
# Re-run `agentbox init` or `agentbox switch --agent <name>` to regenerate this file.
# Do not edit this file directly. Use .agent-sandbox/compose/user.override.yml or user.agent.codex.override.yml instead.
services:
  proxy:
    volumes:
      - type: bind
        source: ../policy/user.agent.codex.policy.yaml
        target: /etc/agent-sandbox/policy/user.agent.policy.yaml
        read_only: true
        bind:
          create_host_path: false
    environment:
      - AGENTBOX_ACTIVE_AGENT=codex
  agent:
    image: ghcr.io/mattolson/agent-sandbox-codex@sha256:0abac652b9d7b8857a871ab92a80e7dde2f269df366796718504fe001436b423
    volumes:
      - codex-state:/home/dev/.codex
      - codex-history:/commandhistory
volumes:
  codex-state:
  codex-history:
