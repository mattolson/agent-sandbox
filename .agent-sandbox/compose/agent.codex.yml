# Managed by agentbox.
# Re-run `agentbox init` or `agentbox switch --agent <name>` to regenerate this file.
# Do not edit this file directly. Use .agent-sandbox/compose/user.override.yml or user.agent.codex.override.yml instead.
services:
  proxy:
    volumes:
      - ../policy/user.agent.codex.policy.yaml:/etc/agent-sandbox/policy/user.agent.policy.yaml:ro
    environment:
      - AGENTBOX_ACTIVE_AGENT=codex
  agent:
    image: ghcr.io/mattolson/agent-sandbox-codex@sha256:7d06e3cf15e2ec7371f70d4369dc214a5065e71797af0e41a9ecfd3d6d1c1a8c
    volumes:
      - codex-state:/home/dev/.codex
      - codex-history:/commandhistory
volumes:
  codex-state:
  codex-history:
