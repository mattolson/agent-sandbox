# Managed by agentbox.
# Re-run `agentbox init` or `agentbox switch --agent <name>` to regenerate this file.
# Do not edit this file directly. Use .agent-sandbox/compose/user.override.yml or user.agent.gemini.override.yml instead.
services:
  proxy:
    volumes:
      - ../policy/user.agent.gemini.policy.yaml:/etc/agent-sandbox/policy/user.agent.policy.yaml:ro
    environment:
      - AGENTBOX_ACTIVE_AGENT=gemini
  agent:
    image: ghcr.io/mattolson/agent-sandbox-gemini@sha256:d51ec9013684260e296d8ae104d390929ef9345756316fd4c8621f438aca8e07
    volumes:
      - gemini-state:/home/dev/.gemini
      - gemini-history:/commandhistory
volumes:
  gemini-state:
  gemini-history:
