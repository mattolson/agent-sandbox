# GitHub Copilot CLI policy (devcontainer mode)
#
# Allows Copilot CLI, and VS Code infrastructure domains.
# Copy to: ~/.config/agent-sandbox/policies/copilot-devcontainer.yaml

services:
  - copilot
  - vscode
