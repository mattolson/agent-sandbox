# Default proxy policy - blocks all outbound traffic
# Used directly for legacy single-file policy setups and as the base source path.
# Layered CLI setups render the effective policy before mitmproxy starts.
# See docs/policy for details
services: []
domains: []
