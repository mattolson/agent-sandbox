# Claude Code Agent Network Policy
# Extends base policy with Claude Code endpoints.
#
# To customize: mount your own policy at ~/.config/agent-sandbox/policy.yaml

services:
  - github  # Fetches IP ranges from api.github.com/meta

domains:
  # Claude Code
  - api.anthropic.com
  - sentry.io
  - statsig.anthropic.com
  - statsig.com
